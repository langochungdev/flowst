# Kế hoạch code theo thứ tự feature

1. Setup project skeleton + window/tray cơ bản
2. SQLite schema + settings JSON + command CRUD
3. Timer state machine + auto-split algorithm (FE thuần, có unit test)
4. UI cửa sổ chính + mini window always-on-top
5. Debug panel (log + RAM)
6. Dashboard: chart + contribution graph
7. Import/export
8. Build script + release script + Husky hook
